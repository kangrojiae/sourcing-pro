# 이어서 작업하기

새 대화를 시작했다면 이 문서부터 읽으면 됩니다. 지금까지의 상황과 다음에 할 일이 적혀 있습니다.

## 이 프로젝트가 무엇인가

쿠팡에서 팔리는 상품과 1688의 소싱 후보를 나란히 놓고 개당 이익금을 계산합니다.
키워드 발굴 → 쿠팡 노출 순위 → 1688 이미지 검색 → 이익 계산이 한 흐름으로 이어집니다.

- 앱 주소: https://claude.ai/code/artifact/f7a928bd-52ca-4a1d-a03a-dc272f1b365e
- 저장소: https://github.com/kangrojiae/sourcing-pro

## 배포 방법 (중요)

`sourcing-pro-app.html` 을 고친 뒤에는 **반드시 위 앱 주소로 다시 배포**해야 화면이 바뀝니다.
주소를 지정하지 않고 배포하면 별개의 새 앱이 만들어지고, 기존 계정과 기록이 따라오지 않습니다.

## 지금 상태

동작을 확인한 것:

- 로그인, 계정별 보관함, 어드민 콘솔
- 쿠팡 상품 수집 (상품명, 가격, 별점, 리뷰, 썸네일)
- 쿠팡 검색 노출 순위 20개 수집 (광고 제외)
- 네이버 검색광고 키워드 조회 (검색량, 광고 경쟁도, 붙은 광고 수로 판정)
- 자동 소싱 (개수와 쿠팡 판매가 범위를 정하면 키워드 → 쿠팡 → 1688 을 한 번에 돌린다)
- 네이버 금융 환율 자동 반영
- 이익 계산 전체
- 쿠팡 화면 위 28일 딱지 (확장프로그램 `overlay.js`). 쿠팡 검색 목록 카드마다 윙의 최근 28일
  판매량·조회수를 띄우고 매출·전환율을 계산한다. 딱지는 반투명, 네 모서리로 끌어 옮긴다.
- 카테고리 베스트. 쿠팡 첫 화면 차림표를 읽어 대·중·소분류 선택창을 만들고, 소분류를 고르면
  가격대·판매량순 주소로 쿠팡을 열어 읽은 뒤 윙 28일 판매량으로 정렬한다.
- 테무 베스트 (베스트셀러·별점 5점·신상품, 갈래 14개), 타오바오 베스트 (판매량순, 한글은 중국어로 번역).
- 상품 출처 구분. 비교 화면 왼쪽 칸이 쿠팡·타오바오·테무에 따라 이름과 색이 바뀐다.

아직 실제 화면에서 확인하지 못한 것 (쿠팡은 Claude 브라우저 도구에서 차단되어 직접 볼 수 없다):

- **리뷰 분석 (v3.53~3.57).** 쿠팡 목록 딱지와 상세 화면 오른쪽 아래 단추, 앱의 노출 순위·카테고리
  베스트 표에 있다. 최근 리뷰 100개로 좋은 점·나쁜 점·수입해서 노릴 점을 뽑는다.
  사용자가 한 번 해봤을 때 리뷰 0개가 나와, 네 가지 방법을 차례로 시도하게 고쳤고 결과는 아직 모른다.
  실패하면 창에 "탭/DATE_DESC 0개 · 바로/..." 식으로 시도 내역이 찍힌다.
- 딱지를 상품 한 칸 전체에 붙이도록 바꾼 v3.57 의 위치.

아직 확인이 덜 된 것:

- **1688 이미지 검색.** 확장프로그램 v3.8에서 대기 시간을 줄였으나 결과를 확인하지 못했습니다.
  쿠팡 썸네일을 1688 이미지 검색에 올려 같은 물건을 찾고, 가격이 싼 것부터 5개를 채웁니다.
  자동이 막히면 1688 탭이 열린 채 남고, 앱의 "열린 1688 결과 가져오기" 로 직접 가져올 수 있습니다.

## 다음에 할 일

1. **1688 이미지 검색 확인.** 비교 화면에서 "썸네일로 1688에서 찾기" 를 눌러본다.
   실패하면 안내 문구에 숫자가 찍힌다. 그 숫자로 어디가 막혔는지 알 수 있다.
   - 검색 패널 0곳: 업로드 후 뜨는 패널을 못 찾음
   - 버튼 후보 0개: 패널은 찾았으나 버튼 글자를 못 읽음
   - 상품 카드 N개인데 결과 0: 카드는 찾았으나 값을 못 읽음

2. **수수료율표와 배송 규격표 채우기.** `sourcing-pro-app.html` 의 `FEE_TABLE`, `SHIP_TABLE` 이
   비어 있다. 사용자가 목록을 주기로 했다. 채우면 비교 화면에 선택 상자가 생기고
   카테고리나 규격을 고르면 값이 자동으로 들어간다.

   ```js
   var FEE_TABLE = [{name:'영상가전 > 영상액세서리', rate:5.8}];
   var SHIP_TABLE = [{name:'극소형', fee:2000}];
   ```

3. **자기 도메인으로 옮기기.** 사용자가 원하고 있다. 지금은 claude.ai의 저장소와 로그인을
   쓰고 있어 그대로는 못 옮긴다. 저장소, 이미지 보관, 인증을 바꿔야 한다.
   Vercel + Supabase 조합을 제안했으나 아직 정하지 않았다.

## 알아둘 점

- **네이버 쇼핑 검색 API 는 2026년 7월 31일에 폐지됐다. 대체 API 가 없다.**
  일반 검색만 네이버 클라우드의 API HUB 로 옮겨졌고 쇼핑·책·전문자료 검색은 없어졌다.
  붙였다가 되돌렸으니 다시 시도하지 말 것. 네이버 검색광고 API(키워드 검색량)는 그대로 살아 있다.

- **상품을 찾는 통로는 셋뿐이다.** 쿠팡 파트너스 API(승인·실적 조건), 직접 모드(사람이 검색),
  화면 읽기 자동 모드(막힐 수 있음). 파트너스 승인 전까지는 직접 모드가 기본이다.

- **쿠팡은 파트너스 공식 API 를 먼저 쓴다 (확장프로그램 v3.18).**
  `api-gateway.coupang.com` 의 affiliate_open_api 를 HMAC-SHA256 CEA 서명으로 부른다.
  키는 확장프로그램 설정에 넣는다. **검색은 한 시간에 열 번, 한 번에 열 개까지다.**
  한도를 넘기면 알려주고 멈춘다. 키가 없으면 예전처럼 화면을 읽되 그때는 막힐 수 있다.
  응답 필드 이름은 실제로 한 번도 받아보지 못했다. 연결 테스트가 실패하면
  `partnerSearch` 의 필드 매핑부터 확인할 것.

- **쿠팡 수집은 직접 모드가 기본이다 (확장프로그램 v3.17).** 자동으로 쿠팡을 열면 결국 막힌다.
  직접 모드는 사용자가 쿠팡에서 검색하고 앱은 그 화면만 읽는다. 막힐 근거가 없다.
  자동 모드도 남아 있지만 홈에서 검색창에 입력하는 방식으로 바꿨고, 읽은 결과는
  반나절 동안 저장해 같은 키워드로 다시 열지 않는다.
  프록시나 지문 위조는 쓰지 않는다. 더 확실한 길은 쿠팡 파트너스 공식 API 이고 아직 붙이지 않았다.

- **1688 후보는 사진과 이름 두 갈래로 찾는다.** 이름 검색을 먼저 돌려 바닥을 깔고,
  사진 검색이 되면 그 결과를 위에 얹는다. 다섯 자리 중 사진에 최대 셋, 나머지는 이름이다.
  후보 카드에 출처 표시가 붙으므로 이름 쪽은 같은 물건인지 확인해야 한다.

- **쿠팡은 요청이 몰리면 아이피를 막는다 (확장프로그램 v3.10).** 그래서 페이지는 4초 간격,
  썸네일은 3장씩 간격을 두고 받는다. 받은 썸네일은 기억해 두 번 받지 않는다.
  차단 화면을 만나면 3분 동안 쿠팡 요청을 아예 보내지 않는다.
  값은 `background.js` 의 `PAGE_GAP_MS`, `IMG_GAP_MS` 에 있다.
  자동 소싱은 이 간격 때문에 열 개에 5~10분이 걸린다. 줄이면 다시 막힌다.

- **확장프로그램 폴더가 두 벌 있다. 고친 뒤 반드시 복사해야 한다.**
  작업하는 곳은 `~/Downloads/sourcing-pro-폴더/sourcing-pro-extension` 이지만,
  크롬이 실제로 읽는 곳은 `~/Downloads/sourcing-pro-project/sourcing-pro-extension` 이다.
  크롬에 등록된 주소를 바꾸면 확장프로그램 ID 가 바뀌어 저장해 둔 검색광고 API 키와
  페이지 연결이 모두 풀리므로, 등록된 주소는 그대로 두고 파일만 덮어쓴다.

  ```
  cp ~/Downloads/sourcing-pro-폴더/sourcing-pro-extension/{background.js,bridge.js,manifest.json,scrape.js,overlay.js,offer1688.js,popup.js,popup.html,options.js,options.html,fxscrape.js} ~/Downloads/sourcing-pro-project/sourcing-pro-extension/
  ```

- **1688 수집 전에 로그인을 먼저 확인한다 (확장프로그램 v3.8).** 1688 은 로그인하지 않으면
  상품 목록을 내주지 않아 수집이 조용히 실패했다. 지금은 쿠키(`__cn_logon__`)만 읽어 즉시 판단하고,
  안 되어 있으면 로그인 안내 모달을 띄운다. 쿠키를 읽을 수 없으면 막지 않고 그대로 진행한다.

- **이미지 검색 대기가 60초에서 22초로 줄었다.** 확인 간격도 1.5초에서 0.6초로 좁혀,
  기다리는 시간은 줄고 확인 횟수는 오히려 늘었다. 값은 `background.js` 맨 위의
  `IMG_POLL_MS`, `IMG_CLICK_WAIT_MS`, `IMG_BUDGET_MS` 에 모여 있다.

- **네이버쇼핑은 더 이상 열지 않는다 (확장프로그램 v3.7).** 예전에는 키워드마다 네이버쇼핑
  검색 결과 탭을 열어 등록 상품 수를 셌는데, 탭이 연달아 열리면서 네이버가 접속을 일시 차단했다.
  지금은 검색광고 API 가 주는 광고 경쟁도와 평균 광고 노출 수만으로 경쟁을 판정한다.
  다시 상품 수를 쓰고 싶다면 탭을 여는 방식 말고 다른 경로를 찾아야 한다.

- **지금 로그인은 진짜 인증이 아니다.** 저장소가 조직 구성원에게 공유되는 공간이라,
  비밀번호를 해시로 저장해도 원본 데이터는 읽힌다. 도메인으로 옮길 때 제대로 바꿔야 한다.
- **확장프로그램 없이도 앱은 동작한다.** 수집만 안 될 뿐 직접 입력하면 계산은 그대로 된다.
- **API 키는 브라우저에만 저장된다.** 컴퓨터마다 한 번씩 넣어야 한다.
- **1688과 쿠팡은 화면 구조를 자주 바꾼다.** 수집이 깨지면 선택자가 아니라
  화면 구조에 기대지 않는 방식으로 고치는 편이 오래간다.
  실제로 링크를 찾는 방식에서 "그림과 가격을 함께 가진 가장 안쪽 상자" 를 찾는 방식으로 바꿨다.

- **윙 28일 판매량은 윙 탭 없이 받는다 (v3.50).** `wing.coupang.com/tenants/seller-web/pre-matching/search`
  에 상품번호를 keyword 로 POST 하면 `salesLast28d`, `pvLast28Day` 가 온다. XSRF-TOKEN 쿠키를 헤더로 싣는다.
  서비스워커에서 바로 부르고, 막히면 열린 윙 탭 안에서, 그것도 없으면 고정 탭을 뒤에 만들어 부른다.
  참고한 WingMeter28 은 쿠키 권한이 없어 윙 탭이 반드시 열려 있어야 했다.

- **쿠팡 리뷰는 `www.coupang.com/next-api/review` 로 받는다.** 쿠팡 리뷰 추출기 확장프로그램 코드에서
  확인했다. `productId, page, size(최대 50), sortBy(기본 ORDER_SCORE_ASC), ratingSummary=true`.
  응답 `rCode === 'RET0000'`, 리뷰는 `rData.paging.contents`. 최신순 정렬 값 `DATE_DESC` 는 검증 전이다.
  문장 정리는 앱 안의 언어 모델만 할 수 있어서, 쿠팡 화면에서 누르면 수집기가 열린 소싱 프로 탭에
  정리를 부탁한다 (`spAnalyze` → bridge → 앱 `analyze`). 소싱 프로 탭이 없으면 원문만 보여준다.

- **1688 상품 화면에서 사진·정보·옵션을 담는다 (v3.58, `offer1688.js`).** 상세 화면 오른쪽 아래 단추.
  값은 페이지 데이터 `window.context.result` 에 있어 MAIN 세계로 읽는다 (`read1688InPage`).
  대표 사진 `offerDetail.imageList[].fullPathImageURI`, 옵션 `offerDetail.skuProps`, 옵션별 가격·재고
  `tradeModel.skuMap`, 속성 `offerDetail.featureAttributes`, 옵션별 크기·무게
  `detailDescription.pieceWeightScale`, 상세 사진 `window.offer_details.content`, 영상 `wirelessVideo.videoUrls`.
  파일은 다운로드 폴더 `1688/<상품번호_상품명>/` 에 대표_01, 옵션_01_이름, 상세_01, 영상, 상품정보.txt, 옵션.csv 로 받는다.
  2026-09-17 실제 상품(916184375338)에서 읽기와 사진 원본 크기 로딩까지 확인했다. 다운로드 자체는 사용자 확인 전.

- **화면을 그려야 하는 작업은 옆창에서 한다 (v3.46~3.47).** 배경 탭은 크롬이 멈춰 테무·1688 이미지 검색이
  실패했다. 초점을 주지 않는 창을 하나 띄워 계속 재사용하고, 100초 쉬면 닫는다 (`openSideTab`).
  사용자 화면에서 완전히 숨기는 방법은 없다고 설명했다.

- **버튼을 연결할 때는 없는 요소를 잡지 않게 한다.** 출처에 따라 숨긴 버튼을 `$('#id').onclick` 으로 잡다가
  오류가 나 그 뒤 연결이 통째로 죽은 적이 있다. `bindCompare` 는 `on(id, fn)` 으로 있는지 보고 연결한다.

## 사용자에 대해

- 수입 셀러이자 빅셀 창업자다. 상품을 개발하지 않고, 잘 팔리는 상품을 찾아 중국에서 들여와 판다.
  발주~판매~출고~매출은 빅셀이 맡고, 소싱 프로는 소싱 자동화만 맡는다. 오토는 나중 일이다.
- 다음에 권한 일: 발굴 표에 매입 상한가(쿠팡 판매가 역산 ¥)를 찍어 돈이 되는 상품만 남기기.
  수수료율표·배송비표가 먼저 필요하다.

- 한국어로 대화한다.
- 개발자가 아니다. 터미널 명령은 붙여넣을 수 있게 한 줄로 주는 편이 좋다.
- 윈도우 노트북과 맥을 오간다. 저장소로 동기화한다.
- 확장프로그램 설치는 크롬 개발자 모드로 직접 한다.
